# 《Forge 项目架构审核报告》复核情况说明

> 复核对象：`Forge-架构审核报告.md`（Claude Code Opus 4.8 生成，日期 2026-06-26）
> 复核方式：逐条对照真实代码库核验，所有数值均来自实测命令
> 复核日期：2026-06-26
> 结论：**报告的架构洞察方向可靠，但存在 6 处论据/数据错误，其中 3 处直接动摇对应行动项建议。**

---

## 一、需修正的错误项（按影响排序）

### ❌ 错误 1 —— dist/ 入库声明（核心论点错误）

| 项 | 内容 |
|----|------|
| **报告原文** | 第四节：「**dist/ 入库（3555 文件）** + sync 校验……git 历史膨胀、PR diff 噪音、合并冲突频发」「建议改为发布时 CI 产出，而非入库」 |
| **实测** | `git ls-files dist` 仅返回 **4 个文件**：`dist/forge-context.mjs` + `dist/security/adversarial-corpus/evaluate.{d.ts,js,js.map}`。其余 dist 内容均在 `.gitignore`（`dist/src/`、`dist/scripts/`、`dist/test/`、`dist/claude-code/bundles/` 等全部 ignore）。3555 是**工作树文件数，非 git 跟踪数**。 |
| **证据** | `.gitignore:3-18`；`git ls-files dist` 输出仅 4 行 |
| **影响** | 整段"dist 入库导致 PR 噪音/冲突"的因果链不成立。报告据此推出的行动项 #5「dist/ 移出 git 改 CI 产出」**前提缺失**——实际只入了极小子集，"移出"几乎无收益。 |
| **修正建议** | 删除第四节关于 dist 入库的批评与对应行动项；如要批评 sync 流程，应改为针对"157 次 sync derived 提交"本身的频率问题，而非错误归因到全量入库。 |

---

### ❌ 错误 2 —— Methodology「无差异化实现」（核心论点错误）

| 项 | 内容 |
|----|------|
| **报告原文** | 第三节技术债 #3：「`Methodology` 类型定义了华为 5-Why、Musk 5-step、Amazon Backwards 等 8 种方法论，但 `src/` 里**只是类型标签，无差异化实现**。」 |
| **实测** | `src/pua-engine.ts:171` 的 `METHODOLOGY_DESCRIPTIONS: Record<Methodology, string>` 为**每一种方法论都提供了完整 5-step prompt 实现**：Huawei 5-Why、Musk Algorithm、Amazon Backwards、ByteDance A/B、Alibaba Closure、Netflix Keeper、Baidu Search 等均有差异化的具体步骤文本，并非空标签。 |
| **证据** | `src/pua-engine.ts:171-230`；`src/schemas/review-report.ts:28-36` |
| **影响** | 该论据支撑的"假抽象"批评不成立。对应行动项 #6「Methodology 类型落地或删除」中"落地"这一半**已经完成**。 |
| **修正建议** | 撤销"无差异化实现"表述；若仍要批评，可改为"方法论仅落地为 prompt 文本，无运行时分支逻辑"，但需重新评估是否构成技术债。 |

---

### ❌ 错误 3 —— frontmatter「各写一遍」（核心论点错误）

| 项 | 内容 |
|----|------|
| **报告原文** | P1-1：「frontmatter 解析在 ≥3 个模块各写一遍（`src/frontmatter.js`、`decide/adr.ts`、`status-manager.ts`）」 |
| **实测** | 存在**集中模块** `src/frontmatter.ts`（导出 `parseFrontmatter` / `extractStringField`）。`decide/adr.ts` 和 `status-manager.ts` **均通过 `import { parseFrontmatter } from "../frontmatter.js"` 复用**，并未各自重复实现。 |
| **证据** | `src/frontmatter.ts`；`src/decide/adr.ts:8`；`src/status-manager.ts:14` |
| **影响** | P1-1 的"frontmatter 重复"论据**不成立**。该 P1 仅".forge/ 路径散布"这一点有效（实测 103 个 ts 文件硬编码，但报告说"15+"偏保守）。 |
| **修正建议** | 将 P1-1 收窄为".forge/ 路径常量散布 103 文件，无集中常量"；删除 frontmatter 重复实现的表述。 |

---

### ❌ 错误 4 —— cmux-integration「找不到实现」

| 项 | 内容 |
|----|------|
| **报告原文** | 第三节技术债 #2：「`.forge/specs/cmux-integration/design.md`（2159 行）……在 `src/` **找不到对应实现**」 |
| **实测** | src/ 中有 **10 个相关文件**：`src/forge-dispatcher/cmux-gate.ts`、`src/harness-cmux-browser.ts`，以及 `allowlist.ts`、`audit-log.ts`、`accept-driver.ts`、`cli-harness.ts`、`harness-detector.ts`、`ui-harness.ts`、`frontend-check.ts`、`check-frozen.ts` 中均有 cmux 引用。cmux **已实际落地**。 |
| **证据** | `grep -rln "cmux" src --include='*.ts'` 返回 10 个文件 |
| **影响** | 规格漂移清单**应从 2 个缩减为 1 个**。报告据此主张的"457 spec 需要实现验证清理"力度被夸大。 |
| **修正建议** | 漂移清单仅保留 `cursor-team-kit/design.md`（该项实测确为 0 引用，正确）；删除 cmux-integration。 |

---

### ❌ 错误 5 —— 知识库「88K 行」数据错误

| 项 | 内容 |
|----|------|
| **报告原文** | 第三节技术债 #2：「457 个 spec / **88K 行**的知识库需要"实现验证"清理」 |
| **实测** | `.forge/knowledge/` 共 **1826 行 / 30 文件**（扣除 sessions 子目录后 1639 行）。 |
| **证据** | `find .forge/knowledge -name '*.md' \| xargs wc -l` → 1826 total |
| **影响** | 数量级错误（差 ~48 倍），疑似把 specs 与 knowledge 混算。"88K 行"会误导维护者高估知识库治理成本。 |
| **修正建议** | 改为「457 个 spec（含 147 个 design.md）+ 1826 行知识库」。 |

---

### ❌ 错误 6 —— @deprecated 数量错误

| 项 | 内容 |
|----|------|
| **报告原文** | 第三节技术债 #5：「**27 个** `@deprecated` 未清理：`sandbox-policy.ts` 占 8 个」 |
| **实测** | 全 src/ 共 **11 个** `@deprecated`：`sandbox-policy.ts` 8 + `forge-read.ts` 2 + `check-sandbox.ts` 1。 |
| **证据** | `grep -rn "@deprecated" src --include='*.ts' \| wc -l` → 11 |
| **影响** | 数值夸大近 2.5 倍。"sandbox-policy 占 8 个"这一点本身正确。 |
| **修正建议** | 改为「11 个 `@deprecated`，其中 sandbox-policy.ts 占 8 个」。 |

---

## 二、数值偏差（方向正确，数值偏高/偏低）

这些条目观察方向成立，但具体数值需校准：

| # | 报告声称 | 实测 | 偏差 |
|---|---------|------|------|
| 1 | commit 中 L0-L8/层 等术语出现 **48 次** | 全历史 commit 共 **18 次** | 偏高 ~2.7 倍，但"术语膨胀"方向性观察成立 |
| 2 | check 链 **~20 个**串联校验 | `scripts/check-*` 实际 **39 个** | 偏保守 |
| 3 | `router.ts` **595 行** | 实际 **594 行** | 差 1 行（可忽略） |
| 4 | `router.ts:125` 的 `_intentDictCache` | 实际在 **123 行** | 差 2 行（可忽略） |
| 5 | `.agents/` **仅 1 文件** | 实际 12K，含 `skills/` 子目录 | 不准确 |

---

## 三、核实通过、可原样保留的部分

以下声明已逐一验证为**准确**，报告对应内容可直接采信：

- 基础数据：v3.9.0 / 63K 行 TS（63034）/ 709 测试文件
- **P0-1**：`state-machine/` 全库零引用（实测 0）、`skill-scheduler` 自承认非生产路径（注释原文 `NON-PRODUCTION path — it has no callers in src/`）
- **P0-2**：`accept-driver.ts` 1219 行/40 函数、`plan.ts` 1127 行/46 导出、`router.ts` 全局可变缓存 `_intentDictCache`
- **P1-2**：`tryParseCiEvidence()` 位于 `fallback.ts:289`，L0-L3 降级链结构属实
- 安全亮点全部属实：forge-exec 三层防御（allowlist `:92` + deny `:9-44` + 元字符检测 `:179-200`，用 `execFile`）、HMAC 链式审计（`createHmac`/`.audit-secret`/`prev_hmac` + 文件锁）、path-validator 三重校验（`resolve+relative+realpathSync`）、对抗语料库 + 确定性 bypass 评估（`evaluate.ts:249`）
- `.kiro/` 828K、`.codex/` 124K
- Stryker：装依赖无配置文件（无 `stryker.conf.*`）
- Phase 1 sandbox 为 advisory-only（注释明确 `advisory-mode` vs `runtime enforcement`）
- specs 共 457 个 .md（含 147 个 design.md）；cmux design 2159 行
- evolved-rules：9 字段、Verified_via/Baseline_violation 多填 N/A、14 条规则接近 15 上限
- docs-governance 最大文件 261 行（`bilingual.ts`）
- 低 ROI check 脚本确实存在（`check-readme-metrics` / `check-evolution-marker-zones` / `check-agent-originality`）
- `cursor-team-kit/design.md` 在 src/ 中确为 0 引用（真正的规格漂移）

---

## 四、复核总评

| 维度 | 评价 |
|------|------|
| **架构洞察** | 可靠。状态机冗余、上帝对象、L2 语义漏洞、僵尸目录、概念膨胀的判断均与代码吻合 |
| **安全亮点评价** | 准确。三层防御、HMAC 审计、对抗语料库等描述与实现一致 |
| **数据严谨性** | **有明显瑕疵**。6 处论据/数据错误，其中错误 1/2/3 直接动摇对应行动项 |
| **行动项可信度** | #1、#2、#4 可直接采纳；#3 需收窄；#5 前提缺失；#6 部分失效 |

**一句话**：报告的"判断力"过硬，但"数据准确性"需打折——采纳前请用本说明替换其中的错误数据与失效论据，避免维护者按错误前提执行无效重构（如统一 frontmatter、移除已入库的 dist、为 Methodology 补落地）。
