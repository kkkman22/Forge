## Layer 2 -- Code Quality

**Reviewer**: quality-check
**Scope**: `src/context-budget.ts`, `src/index.ts`, `test/context-budget*.test.ts`, `test/barrel-file.test.ts`

| # | Severity | File | Issue | Suggestion |
|---|----------|------|-------|------------|
| 1 | P1 | `src/context-budget.ts:536` | `deserializeSubagentSummary` changedFiles regex contains escaped-when-should-be-literal negative lookahead `(?!\\(none\\))` -- the double-backslash matches the literal characters `\(\n\o\n\e\\)`, not the intended `(none)`. The secondary `!line.includes("(none)")` guard prevents incorrect parsing, but the regex itself is dead code and misleading. | Remove the broken lookahead: `line.match(/^\s*变更文件：(.+)$/u)`; keep the `includes` guard, or fix the lookahead to `(?!\\(none\\))` -> `(?!\\(none\\))` (single backslash in source). |
| 2 | P1 | `src/context-budget.ts:229,456,528` | Three unsafe `as` type casts in deserializers. `m[1] as "P0"..."` at line 229, `m[1].toLowerCase() as "staged"..."` at line 456, `m[1].trim() as SubagentSummary["status"]` at line 528. If the regex captures an unexpected value (e.g., due to format evolution or injection), the cast silently produces an invalid type with no runtime guard. | Add runtime validation before the cast. E.g., for severity: `const valid = ["P0","P1","P2","P3"]; if (!valid.includes(m[1])) continue;`. Same pattern for status and category. |
| 3 | P1 | `test/context-budget-roundtrip.property.test.ts:94` | `reviewSummaryArb` uses unconstrained `fc.string({ minLength: 1, maxLength: 80 })` for top-level `filePath`, while `findings[].filePath` uses the filtered `pathArb`. Unconstrained strings may contain `)` or full-width `）` characters that break the deserializer's `headerMatch` regex (`详见 (.+?)）`), causing non-deterministic round-trip test failures. | Replace `fc.string({ minLength: 1, maxLength: 80 })` with `pathArb` for the top-level `filePath` field, or add `)`, `）`, `：` to the filter. |
| 4 | P2 | `src/context-budget.ts:620-672` | `serializeContextBudgetReport` / `deserializeContextBudgetReport` have zero round-trip or property tests. Only the barrel-file export test verifies their existence. | Add a round-trip property test in `context-budget-roundtrip.property.test.ts` with a `contextBudgetReportArb` generator. |
| 5 | P2 | `src/context-budget.ts:96-160` | Deserializers silently return partial/default objects when regexes fail to match. `deserializeExploreSummary("")` returns an object with all-empty arrays and no indication of failure. Callers cannot distinguish "parsed successfully with empty data" from "parse failed completely." | Consider returning a `{ ok: boolean; value: T }` discriminated union, or at minimum document that partial results are expected and callers should validate critical fields. |
| 6 | P2 | `src/context-budget.ts:226` | `findings` regex `(P[0-3]): (.+):(\d+)` uses greedy `(.+)` for filePath. Paths containing `:` followed by digits (e.g., `src/v2:port/module.ts`) would be incorrectly parsed -- the greedy `.+` matches too far, and `:\d+` anchors to the rightmost `:digits` in the line. | Make filePath non-greedy `(.+?)` and anchor the numeric suffix, or use a more specific character class like `([^:]+(?:[:/][^:]+)*)` to handle paths with colons. |
| 7 | P2 | `src/context-budget.ts:350-367` | `serializeGitDiff` takes a `lineCount` parameter but the short-form branch (lineCount <= 50) does NOT include individual file entries. If the caller passes a low `lineCount` but the deserializer expects file entries, the round-trip loses file-level data silently. The current round-trip test only tests `lineCount=51`. | Add a round-trip test for the short-form path (lineCount <= 50) and document in JSDoc that the short-form omits per-file details. Alternatively, always include per-file entries. |
| 8 | P2 | `test/context-budget.property.test.ts:107,122` | `_gitDiffSummaryArb` and `_gitStatusSummaryArb` are declared with underscore-prefixed names but never used. Dead code in test files. | Remove unused generators, or use them in Property 5 tests to add property-based coverage instead of only hand-crafted examples. |
| 9 | P2 | `src/context-budget.ts` (all deserializers) | Six deserializer functions follow the identical pattern: split text into lines, loop with regex matching, accumulate into result. This pattern is repeated verbatim 6 times with no shared infrastructure. | Extract a shared `parseLines<T>(text, patterns: LinePattern<T>[]) => T` helper that encapsulates the split-and-match loop. Reduces duplication and centralizes future fixes (e.g., adding validation). |
| 10 | P2 | `src/context-budget.ts:350` | `serializeGitDiff` ignores `summary.files` entirely in the short-form branch (lineCount <= 50). The `lineCount` parameter is also not part of the `GitDiffSummary` interface, creating an inconsistent API where serialization depends on external context. | Move `lineCount` into `GitDiffSummary` or into a separate options bag, so all data needed for serialization is self-contained. |
| 11 | P2 | `src/context-budget.ts:413` | Same issue for `serializeGitStatus` -- the `fileCount` parameter is external to `GitStatusSummary`. The short-form branch (fileCount <= 30) omits the file list entirely, losing data silently. | Same suggestion as #10: embed threshold data in the interface or an options bag. |
| 12 | P2 | `test/context-budget.property.test.ts` | Property tests for git-diff and git-status (Property 5) use only hand-crafted example data, not property-based generators, despite `_gitDiffSummaryArb` and `_gitStatusSummaryArb` being defined. This means token-budget constraints and edge cases are not fuzz-tested for these serializers. | Convert Property 5 tests to use `fc.property` with the existing generators, replacing or supplementing the hand-crafted examples. |
| 13 | P3 | `src/context-budget.ts` (throughout) | Regex patterns in deserializers contain Chinese characters and Unicode without comments explaining their format expectations. Future maintainers cannot tell what format the regex is meant to parse without reading the serializer. | Add inline comments above each regex showing an example of the expected input format, or add a block comment at the top of each serializer/deserializer pair showing input/output examples. |
| 14 | P3 | `src/context-budget.ts:456` | `m[1].toLowerCase()` maps "Staged" -> "staged", "Modified" -> "modified", "Untracked" -> "untracked". While the regex captures the exact English labels, this mapping is not type-safe -- adding a new category to the interface would require updating both the regex and the cast. | Use a lookup map `{ Staged: "staged", Modified: "modified", Untracked: "untracked" } as const` with a runtime check instead of `toLowerCase() + as`. |
| 15 | P3 | `src/context-budget.ts:369-411` | `deserializeGitDiff` file-path regex uses negative content checks (`!line.includes("变更")`, `!line.includes("总计")`, `!line.includes("📊")`) to exclude header lines. This is fragile -- a file path containing any of those Chinese strings would be silently skipped. | Use a more specific positive regex pattern (e.g., require the line to start with exactly two spaces and match `path: +N -N`) instead of negative content filters. |
| 16 | P3 | `src/context-budget.ts:177-195` | `serializeReviewSummary` uses the em-dash character `\u2014` in the finding separator. While this matches the deserializer's regex, it is an easy-to-miss non-ASCII character that could be corrupted during copy-paste or re-encoding. | Consider using a plain ASCII separator like `--` or add a comment noting the em-dash. |

### Summary

- **P1**: 3 findings -- one dead/misleading regex, three unsafe type casts, and an unconstrained test generator that can cause flaky round-trip tests.
- **P2**: 9 findings -- missing ContextBudgetReport round-trip tests, silent partial parse failures, greedy regex edge cases, duplicated deserializer patterns, external threshold parameters, and unused test generators.
- **P3**: 4 findings -- undocumented regex formats, fragile negative-content filters, non-ASCII separator, and `toLowerCase` cast fragility.

**Recommendation**: Fix all P1 findings before shipping. P2 findings should be addressed in a follow-up unless the team considers specific ones critical for the current release.
