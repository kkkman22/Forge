---
feature: context-optimization
layout: tasks
created: 2026-05-01
spec_ref: ".tinkerman/specs/context-optimization/requirements.md"
---

# Tasks

## Task 1: Add MCP SDK dependencies

- [x] 1.1 Add `@modelcontextprotocol/sdk` and `zod` to `package.json` dependencies
- [x] 1.2 Run `npm install` and verify no dependency conflicts
- [x] 1.3 Run `npx tsc --noEmit` to verify TypeScript can resolve the new modules

## Task 2: Implement output trimmer module

- [x] 2.1 Create `src/mcp/trimmers/output.ts` with `trimCommandOutput(stdout, stderr, exitCode)` function: exit ≠ 0 → full output; exit 0 + ≤30 lines → full output; exit 0 + >30 lines → key line extraction + last 5 lines + stats
- [x] 2.2 Create `src/mcp/trimmers/git.ts` with `parseDiffStat(output)` returning `GitDiffSummary` and `parseStatusPorcelain(output)` returning `GitStatusSummary`, reusing format from `context-budget.ts`
- [x] 2.3 Write unit tests in `test/mcp/output-trimmer.test.ts`: threshold boundary (30 lines exactly → pass through, 31 lines → trim), key line pattern matching, failure passthrough, empty output
- [x] 2.4 Write unit tests in `test/mcp/git-trimmer.test.ts`: diff stat parsing, status porcelain parsing, empty diff, empty status
- [x] 2.5 Write property tests in `test/mcp/output-trimmer.property.test.ts` for Property 1 (failure output completeness) and Property 2 (trim threshold behavior)

## Task 3: Implement forge_exec tool

- [x] 3.1 Create `src/mcp/tools/forge-exec.ts` with tool registration function accepting `McpServer` instance, using `z.object({ command: z.string(), timeout: z.number().optional().default(30000) })` input schema
- [x] 3.2 Implement subprocess execution via `child_process.execFile("/bin/sh", ["-c", command])` with timeout handling (kill on timeout, return error)
- [x] 3.3 Integrate deny rule checking: read deny patterns from `.claude/settings.json` permissions, check command before execution, return `isError: true` if denied
- [x] 3.4 Apply `trimCommandOutput` to subprocess result before returning MCP response
- [x] 3.5 Write unit tests in `test/mcp/forge-exec.test.ts`: successful small output, successful large output trimming, failure passthrough, timeout, deny rule blocking

## Task 4: Implement forge_git tool

- [x] 4.1 Create `src/mcp/tools/forge-git.ts` with tool registration function, using `z.object({ subcommand: z.enum(["diff", "status", "log"]), args: z.string().optional() })` input schema
- [x] 4.2 Implement diff subcommand: execute `git diff --stat`, parse with `parseDiffStat`, format with `serializeGitDiff` pattern
- [x] 4.3 Implement status subcommand: execute `git status --porcelain`, parse with `parseStatusPorcelain`, format with `serializeGitStatus` pattern
- [x] 4.4 Implement log subcommand: execute `git log --oneline -20` (or user args), return directly
- [x] 4.5 Write unit tests in `test/mcp/forge-git.test.ts`: diff summary format, status summary format, log passthrough, git command failure

## Task 5: Implement forge_read tool

- [x] 5.1 Create `src/mcp/tools/forge-read.ts` with tool registration function, using `z.object({ paths: z.array(z.string()), script: z.string(), language: z.enum(["javascript", "shell"]).default("javascript") })` input schema
- [x] 5.2 Implement file path injection via `FORGE_FILES` environment variable (JSON array of paths) and subprocess execution
- [x] 5.3 Implement output isolation: return only stdout, verify file contents are not in response
- [x] 5.4 Write unit tests in `test/mcp/forge-read.test.ts`: script execution with file paths, stdout-only return, error handling
- [x] 5.5 Write property test in `test/mcp/forge-read.property.test.ts` for Property 5 (output isolation): for random file contents and scripts, response never contains raw file content

## Task 6: Implement MCP server entry point

- [x] 6.1 Create `src/mcp/server.ts`: instantiate `McpServer`, register three tools from Task 3-5, connect via `StdioServerTransport`
- [x] 6.2 Add error handling: `process.on("unhandledRejection")` and `process.on("uncaughtException")` to prevent silent crashes
- [x] 6.3 Add `#!/usr/bin/env node` shebang and verify `dist/src/mcp/server.js` is generated by `tsc`
- [x] 6.4 Write integration test in `test/mcp/server.integration.test.ts`: spawn server process, send MCP `initialize` + `tools/list`, verify 3 tools registered, send `tools/call` for forge_exec with `echo hello`, verify response

## Task 7: Update init.sh for MCP configuration

- [x] 7.1 Add MCP configuration step after Hook installation in `scripts/init.sh`: detect `dist/src/mcp/server.js` existence, merge `forge-context` entry into `.claude/settings.json` mcpServers section
- [x] 7.2 Handle edge cases: mcpServers section doesn't exist (create it), forge-context already exists (skip), node not available (warn and skip), settings.json doesn't exist (create with mcpServers only)
- [x] 7.3 Test init.sh manually: fresh init, re-init with existing config, re-init with existing forge-context entry

## Task 8: Update SKILL.md tool call guidance

- [x] 8.1 Update `skills/forge-build/SKILL.md` Context Budget Management section: change Hard Token Limits table entries for test output, git diff, git status, command output to reference `forge_exec` / `forge_git` with server-side trimming
- [x] 8.2 Update `skills/forge-build/SKILL.md` Three-Layer Output Truncation Defense: reorder to forge-context MCP (primary) → run-with-trim.sh (fallback) → AI Trimming Iron Law
- [x] 8.3 Update `skills/forge-test/SKILL.md` Layer 1 and Layer 3: change command execution guidance to use `forge_exec` for test and CI commands
- [x] 8.4 Verify Explore_Summarizer, Review_Summarizer, Subagent_Summary_Protocol sections are unchanged
- [x] 8.5 Run `npm run check` to verify SKILL contract tests still pass

## Task 9: Enhance run-with-trim.sh fallback

- [x] 9.1 Update success path in `scripts/run-with-trim.sh`: replace `tail -10` with key line extraction (`grep -E` for pass/fail/error/warn/coverage/test patterns) + `tail -5`
- [x] 9.2 Verify failure path is unchanged (complete passthrough)
- [x] 9.3 Test manually: run with a passing test suite (>30 lines output), run with a failing test suite, run with small output (<30 lines)

## Task 10: Verify Think in Code implementation (already done)

- [x] 10.1 Verify `agents/explore.md` contains "Think in Code" section with three pre-built script templates
- [x] 10.2 Verify `agents/explore.md` behavior rules include ">5 files → must use Think in Code scripts"
- [x] 10.3 Verify `.claude/agents/explore.md` is synced with `agents/explore.md`

## Task 11: Full regression and integration verification

- [x] 11.1 Run `npm run check` (typecheck + lint + test) — all existing tests must pass
- [x] 11.2 Run MCP server integration test: spawn server, verify tool registration and basic execution
- [x] 11.3 Verify `scripts/init.sh` produces correct `.claude/settings.json` with both hooks and mcpServers sections
- [x] 11.4 Verify Forge workflow functions correctly without MCP server (degraded mode): remove forge-context from settings.json, run a light-tier task, confirm run-with-trim.sh fallback works
