/**
 * Infer CI command suggestion from package.json content.
 * @param {string|null} packageJsonRaw
 * @returns {string|null}
 */
export function suggestCiCommand(packageJsonRaw: string | null): string | null;
