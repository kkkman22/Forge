# Known Bypasses

This category records real injection patterns that have demonstrably escaped
the untrusted-fence defense. Each entry is a sanitized reproduction of an
observed bypass, added when discovered (continues the `known-failures.md`
culture). Append new cases here as they surface.
