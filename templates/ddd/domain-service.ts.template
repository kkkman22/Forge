/**
 * {{ServiceName}} Domain Service
 * {{Description}}
 * Handles cross-aggregate business rules.
 */

import { {{AggregateNames}} } from "./aggregates.js";

export class {{ServiceName}} {
  constructor(
    {{#each Dependencies}}
    private readonly {{this.name}}: {{this.type}},
    {{/each}}
  ) {}

  {{#each Methods}}
  {{this.methodName}}({{this.params}}): {{this.returnType}} {
    // Cross-aggregate business rule: {{this.description}}
    // No persistence concern — delegate to repositories
    {{this.body}}
  }
  {{/each}}
}
