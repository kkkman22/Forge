---
name: "{project-name}"
created: "{date}"
updated: "{date}"
version: "1.0.0"
status: draft
---

# {project-name} — 项目宪章

## 核心问题

> 用 1–3 句话描述本项目要解决的工程问题。
> 例：本系统为电商平台提供实时库存管理，解决多仓库、多渠道场景下的库存一致性
> 和超卖防护问题。

## 架构边界

### 模块划分

> 列出系统的主要模块及其职责。
> 例：
> - **API 层**：对外 HTTP 接口，负责认证、限流、请求验证
> - **业务层**：核心业务逻辑，不依赖具体传输协议
> - **数据层**：通过 Repository Pattern 访问，禁止上层直接写 SQL

### 模块间通信契约

> 描述模块之间的通信方式和约束。
> 例：
> - API 层 ↔ 业务层：同步函数调用，输入/输出使用 DTO
> - 业务层 ↔ 数据层：Repository 接口，返回 domain entity
> - 禁止：API 层直接访问数据层

## 技术选型基线

> 列出项目的核心技术选型。
> 例：
> - **语言**：TypeScript (strict mode)
> - **运行时**：Node.js 20+
> - **框架**：Hono (API)
> - **数据库**：PostgreSQL 16
> - **测试**：Vitest
> - **CI**：GitHub Actions

## 不可变量（Invariants）

> 列出不可违反的工程约束，每条带唯一 ID（INV-NNN）。
> 最多 8 条——超过 8 条说明项目需要拆分而非更多约束。

### INV-001: {invariant-title}

- **规则**：{具体约束描述}
- **理由**：{为什么这条是不可变量}
- **违反后果**：{如果违反会发生什么}

## 约定与偏好（可选）

> 列出命名规范、代码风格、测试策略等。

## 已知的未来变化（可选）

> 列出预期会改变但尚未决定的事项。

## 排除范围（可选）

> 明确不属于本项目的事项。

## 变更日志

| 日期 | 版本 | 变更 | 触发 |
|------|------|------|------|
| {date} | 1.0.0 | 初始创建 | /forge charter init |
