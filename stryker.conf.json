{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "comment": "Audit P2: Stryker was in devDeps but had no config and was never run — mutation testing was theater. This config locks the 3 highest-risk modules first. Run via `npm run mutation`. A nightly CI job (.github/workflows/mutation.yml) executes it.",
  "packageManager": "npm",
  "reporters": ["html", "clear-text", "progress"],
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/router.ts",
    "src/status-manager.ts",
    "src/mcp/tools/forge-exec.ts"
  ],
  "thresholds": {
    "high": 80,
    "low": 60,
    "break": 50
  },
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "ignorePatterns": ["dist", "node_modules", ".worktrees"],
  "timeoutMS": 60000
}
